INSTALLATION GUIDE

The follow steps may have different syntax depending on OS,
however all steps are required regardless of OS:
Step 1: Install MongoDB
Step 2: Install deployd
Step 3: Run browser-sync start --server --browser "Google Chrome" --files "*.css,*.html,*.js"